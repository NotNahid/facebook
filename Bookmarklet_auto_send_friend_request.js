javascript:(function(){const TEXT_TO_FIND="Add friend";const MIN_DELAY=1500;const MAX_DELAY=2500;function sleep(ms){return new Promise(resolve=>setTimeout(resolve,ms))}async function startSweeping(){const buttons=Array.from(document.querySelectorAll('div[role="button"], span[role="button"]'));const targets=buttons.filter(btn=>{const text=btn.innerText?btn.innerText.trim().toLowerCase():"";const label=btn.getAttribute("aria-label")?btn.getAttribute("aria-label").trim().toLowerCase():"";const search=TEXT_TO_FIND.toLowerCase();return text===search||label===search});if(targets.length===0){alert("No %27"+TEXT_TO_FIND+"%27 buttons found!");return}let count=0;for(const btn of targets){if(btn.offsetParent===null)continue;btn.scrollIntoView({behavior:%27smooth%27,block:%27center%27});btn.style.border="4px solid #00FF00";btn.style.backgroundColor="rgba(0, 255, 0, 0.2)";const delay=Math.floor(Math.random()*(MAX_DELAY-MIN_DELAY+1)+MIN_DELAY);await sleep(delay);btn.click();btn.style.border="none";count++}alert("Mission Complete: "+count+" requests sent.");}startSweeping();})();
